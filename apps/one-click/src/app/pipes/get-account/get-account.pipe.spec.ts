import { GetAccountPipe } from './get-account.pipe';

describe('GetAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
