import { GetSocialTypePipe } from './get-social-type.pipe';

describe('GetSocialTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSocialTypePipe();
    expect(pipe).toBeTruthy();
  });
});
